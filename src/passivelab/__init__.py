"""PassiveLab: generation, characterization, optimization and validation of passive structures.

Deliberately empty. The four stable core APIs (`generate` / `characterize` / `optimize` /
`evaluate`, see `docs/ARCHITECTURE.md`) are the deliverable of Phase 1 sub-phase 1.2 and arrive
through the plan and review loop, not by guessing them up front.

Governance: no coding before architecture review; no feature implemented directly in a notebook;
all logic in reusable modules; the notebook only calls APIs.
"""

__version__ = "0.0.0"
