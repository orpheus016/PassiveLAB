Vendored from IHP-GmbH/IHP-Open-PDK (https://github.com/IHP-GmbH/IHP-Open-PDK),
ihp-sg13g2/libs.tech/openems/openems_ihp_sg13g2/workflow/{modules/,SG13G2.xml}.

License: Apache License 2.0 (https://github.com/IHP-GmbH/IHP-Open-PDK/blob/main/LICENSE).
Copyright IHP GmbH -- Leibniz-Institut fuer innovative Mikroelektronik.

Local changes from upstream (PassiveLAB sub-phase 1.4.1):
- modules/util_gds_reader.py, modules/util_meshlines.py, modules/util_simulation_setup.py:
  converted intra-package `import util_x` statements to relative imports (`from . import util_x`)
  so the modules work as a proper Python package under
  `passivelab.characterization.openems.vendor.modules` instead of relying on being added to
  `sys.path` individually.
- modules/util_simulation_setup.py: `runSimulation()` gained an explicit `interactive_preview:
  bool = False` parameter gating the AppCSXCAD GUI-preview launch, replacing the implicit
  `if 1 in excite_portnumbers:` condition. Upstream code (and the golden notebook it was copied
  from) relied on a one-line source edit to disable this for headless/batch runs; this vendored
  copy makes that an explicit, tested parameter instead. No other behavioral changes.

Local changes from upstream (PassiveLAB sub-phase 1.4.6):
- modules/util_gds_reader.py: ported from `gdspy` to `gdstk` (the platform's own chosen GDSII
  library, see `DEC-adopt-gdstk-as-the-l3-generator-backend-0002`) -- upstream's use of `gdspy`
  reintroduced exactly the install friction (no Windows wheel, unmaintained upstream) that ADR
  already replaced it for elsewhere. Output contract (`gds_polygon`/`all_polygons_list`) is
  unchanged; `modules/util_meshlines.py` needed no changes (it only ever consumed those plain
  Python classes, never gdspy's API directly). See `docs/OPENEMS_BACKEND.md` for the full mapping
  and `characterization/openems/tests/test_gds_reader.py` for regression coverage.

Local changes from upstream (PassiveLAB sub-phase 1.4.3):
- modules/util_simulation_setup.py: `runSimulation()` gained optional `num_threads`, `verbose`,
  `dump_statistics` parameters forwarded to `FDTD.Run(excitation_path, **run_kwargs)`. The upstream
  inline comment above that call warns "DO NOT SPECIFY COMMAND LINE OPTIONS HERE! That will fail
  for repeated runs with multiple excitations." -- read as guarding against arbitrary/ad-hoc CLI
  flags, not the narrow, openEMS-documented `numThreads`/`verbose`/`dump_statistics` kwargs added
  here; this backend's caller (`plugin.py`'s per-port loop) already builds a brand-new `FDTD`
  instance per excitation via `setupSimulation()`, so there is no cross-call state for these
  options to corrupt. Confirmed safe by a live multi-port run (see `docs/OPENEMS_BACKEND.md`'s
  "Live validation" section). All three parameters default to `None`/`False`, reproducing the exact
  prior zero-kwarg `FDTD.Run(excitation_path)` call for any other caller.
