"""Per-solver ``SimulationBackend`` implementations. Sibling to ``geometry/`` -- concrete solvers
live here, outside ``core/``, for the same reason concrete devices live outside
``core/geometry/`` (enforced by ``core/tests/test_no_leakage.py``)."""
